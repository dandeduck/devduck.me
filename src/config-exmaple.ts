const firebase = {
  apiKey: "????",
  authDomain: "?????",
  databaseURL: "?????"
}

const emaljs = {
  serviceId: "?????",
  templateId: "?????",
  userId: "?????"
}

const config = {
  firebase: firebase,
  emaljs: emaljs
}

export default config;